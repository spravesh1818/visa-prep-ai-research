# Base URL for the FastAPI backend. Leave empty to use the Vite dev proxy.
VITE_API_BASE=

# Optional: override the proxy target used by `vite dev` (defaults to localhost:8000).
# VITE_API_PROXY=http://localhost:8000
